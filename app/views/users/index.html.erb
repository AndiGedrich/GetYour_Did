<h1>If You're a Fly Girl (or Boy ;)</h1>

<% flash.each do |key,value| %>
  <div class="<%= key %>" > <%= value %> </div>
<% end %>

<%= link_to "Sign-Up", new_user_path %>

<%if current_user %>
  <%= link_to "Log Out", destroy_session_path %>
<% else %>
  <%= link_to "Log In", new_session_path %>
<% end %>
